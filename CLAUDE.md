# 작업 원칙

## 절대 원칙: Higgsfield(힉스필드) 사용 금지

사용자가 **"힉스필드를 이용해줘"** 라고 명시적으로 말하기 전까지는
Higgsfield MCP 서버의 도구를 **어떤 것도 호출하지 않는다.**

- 서버 ID: `bbc65286-55bb-419b-b47b-0fe8894a5016`
- 도구 접두사: `mcp__bbc65286-55bb-419b-b47b-0fe8894a5016__*`
- 해당 서버의 이미지/영상/음성 생성, 음성 복제, 웹사이트 생성, 워크플로 등 **전부 포함**

### 이유
유료 크레딧 기반이며 잔액이 거의 없다. 사용자가 원하지 않는 지출이 발생한다.

### 세부 규칙
- **비용 조회(`get_cost`), 잔액 조회(`balance`), 모델 목록 조회도 호출하지 않는다.**
  "조회만 하는 거니까 괜찮다"는 판단을 스스로 내리지 않는다.
- 위젯을 띄우는 도구(`create_voice`, `show_plans_and_credits`,
  `media_upload_widget` 등)도 마찬가지로 호출하지 않는다.
- TTS, 음성 복제, 영상 제작, 이미지 생성 요청을 받아도 Higgsfield로
  자동 연결하지 않는다. **무료 대안을 먼저 제시한다.**
- Higgsfield가 유일한 방법이라고 판단되면, 실행하지 말고 사용자에게
  먼저 물어본다.

### 해제 조건
사용자가 "힉스필드를 이용해줘"라고 직접 말한 경우에만 사용한다.
한 번 허락받았다고 해서 이후 요청까지 계속 쓸 수 있는 것은 아니다.

---

## TTS / 음성 관련 선호

무료 방식을 우선한다.

- **Qwen3-TTS 오픈소스** — https://github.com/QwenLM/Qwen3-TTS
  (알리바바 Qwen 팀 공식, 로컬 실행 시 완전 무료, 3초 샘플로 음성 복제)
- **Qwen Chat 앱** — 무료 음성 생성
- 영상 편집은 캡컷 등 무료 툴에서 직접 합치는 방식을 기본으로 한다.

## 환경 참고

- `qwen3tts.com` 은 이 실행 환경의 네트워크 프록시에서 차단되어 접속 불가.
- 채팅 첨부 파일은 원격 컨테이너에서 읽을 수 없다.
  파일 전달이 필요하면 Google Drive 또는 공개 URL을 사용한다.
